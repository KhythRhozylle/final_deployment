[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health.html"
healthcheckTimeout = 300
# Railway sets PORT automatically — do not hard-code PORT=8000 in Variables
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
